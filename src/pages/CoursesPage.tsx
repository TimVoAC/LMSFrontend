import { CourseList } from "../components/courses/CourseList";

export const CoursesPage: React.FC = () => {
  return (
    <section>
      <h2>Courses</h2>
      <CourseList />
    </section>
  );
};
